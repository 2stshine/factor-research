[project]
name = "factor-research"
version = "0.1.0"
description = "TeamAlpha Gold — 팩터 발굴·검증 엔진 (Silver 읽기 전용)"
requires-python = ">=3.12"
dependencies = ["pandas", "pyarrow", "numpy", "scipy", "psycopg[binary]", "python-dotenv", "boto3"]

[tool.uv]
package = false
